# התנ״ך הוויזואלי — חבילת השלמה

כל הקבצים כאן מוכנים להעתקה ישירה לריפו. אין תלות בשום ספרייה חיצונית.

## סדר הביצוע — לפי תשואה

### 1. הסירו את החסימה מגוגל  ⏱ דקה
בכל חמשת העמודים יש כרגע:
```html
<meta content="noindex,nofollow" name="robots"/>
```
זו הוראה מפורשת לגוגל **לא** לאנדקס את האתר. כל עוד היא שם, האתר לא יופיע בחיפוש לעולם.
החליפו אותה בבלוק המתאים מתוך `head-snippets.html`.

### 2. תגי שיתוף  ⏱ 10 דקות — זה השינוי הכי משמעותי
כרגע אין באתר אף תג `og:`. משמעות הדבר: קישור שנשלח בוואטסאפ, ב-X, בפייסבוק או
בלינקדאין מופיע כשורת טקסט קירחת, בלי תמונה ובלי כותרת. בישראל וואטסאפ הוא
ערוץ ההפצה המרכזי — וזה בדיוק המקום שבו האתר מאבד את כל הסיכוי שלו להתפשט.

- העתיקו את `assets/og-*.png` אל `assets/` בריפו.
- הדביקו את הבלוק המתאים מ-`head-snippets.html` לתוך ה-`<head>` של כל עמוד.
- בדיקה: https://developers.facebook.com/tools/debug/ ו-https://cards-dev.twitter.com/validator

### 3. סרגל שיתוף  ⏱ 10 דקות
`assets/share.css` + `assets/share.js`. בסוף כל עמוד פרק:
```html
<div class="share-block" data-hook="42,360 — כך אומרת הרשימה. חיבור הפריטים נותן 29,818."></div>
```
ובראש העמוד: `<link rel="stylesheet" href="../../assets/share.css">`
ולפני `</body>`: `<script defer src="../../assets/share.js"></script>`

ה-hook המומלץ לכל פרק:
- לוח השנה — `354 יום בשנה. ולחודשים בתורה אין שמות בכלל.`
- הרכוש — `בית המקדש נבזז 14 פעמים. בחצי מהן שילמנו מרצון.`
- שיבת ציון — `42,360 — כך אומרת הרשימה. חיבור הפריטים נותן 29,818.`

### 4. הלוגו  ⏱ דקה
`assets/atlas-logo.png` שוקל **792KB** ומוצג בגודל 44×44 ו-150×130 פיקסלים.
זה כמעט מגה־בייט שנשלח לכל מבקר בנייד לפני שהוא רואה משהו.
`assets/logo-320.png` (79KB) ו-`assets/logo-96.png` (12KB) זהים חזותית.
החליפו את ה-`src` בסרגל הנייד ל-`logo-96.png` ובסרגל הצד ל-`logo-320.png`.

### 5. robots.txt ו-sitemap.xml  ⏱ דקה
העתיקו את שניהם לשורש הריפו.

---

## מה עוד חוסם, ולא נכלל בקבצים

**״גרסת עבודה · המקורות בבדיקה״** מופיע בכל עמוד ובכל שפה. אתם אומרים לכל מבקר
שהאתר לא גמור. אף אחד לא משתף משהו שמוגדר כטיוטה. או שמסירים את המשפט, או שהופכים
אותו לנכס: ״כל טענה מסומנת במקורה — מקרא, חישוב או שחזור״. זה בדיוק ההבדל בין
התנצלות לבין הבטחה.

**העמודים באנגלית הם מבוי סתום.** שלושת הפרקים ב-EN מובילים חזרה לעברית
(״Explore in Hebrew״). מבקר דובר אנגלית שמגיע ומגלה שאין לו מה לקרוא — יוצא ולא חוזר,
וגם לא משתף. שתי אפשרויות סבירות: לתרגם פרק אחד במלואו ולהשאיר רק אותו,
או להסתיר את מתג השפה עד שיש תוכן. הגרוע מכל הוא המצב הנוכחי.

**כתובת האתר.** `lugasia.github.io/visual-tanakh` לא נראית כמו יעד. דומיין משלכם
עולה כמה עשרות שקלים בשנה, נתמך ב-GitHub Pages בקובץ `CNAME` בשורש, ומכפיל את
הסיכוי שמישהו יעביר את הקישור הלאה. אם תעשו את זה — עדכנו את כל הכתובות
המוחלטות ב-`head-snippets.html` ובקבצי robots ו-sitemap.

**אין מדידה.** בלי אנליטיקס אין דרך לדעת איזה פרק עובד ואיפה אנשים נוטשים.
Plausible או Umami — שורה אחת, בלי עוגיות, בלי באנר הסכמה.

---

## מה יגרום לזה להתפשט — ההערכה שלי

ויראליות בעברית לא מגיעה מעמוד בית יפה. היא מגיעה מ**עובדה אחת שאי אפשר לא להעביר הלאה**.
לאתר הזה כבר יש שלוש כאלה, והן קבורות בעומק הגלילה:

1. בתורה לחודשים אין שמות. השמות שכולנו מכירים — בבליים, וחמישה מהם
   בכלל לא מופיעים בתנ״ך.
2. מלכים אומר שהבית נשרף בשבעה באב. ירמיהו אומר עשרה. תשעה באב אינו אף אחד מהם.
3. הרשימה אומרת 42,360. חיבור הפריטים נותן 29,818.

השלוש האלה צריכות להיות **בראש** כל עמוד פרק, בגופן ענק, לפני כל הסבר — ובדיוק
הן שנכנסו לתמונות השיתוף. אדם שרואה את המשפט השלישי בוואטסאפ לוחץ. אדם שרואה
״מפות, תרשימים וסיפורים אינטראקטיביים מעולם התנ״ך״ — לא.

הצעד הבא, אחרי שהתשתית עומדת: פרק רביעי סביב שאלה שאנשים כבר שואלים —
״איפה ארון הברית״ הוא המנוע החזק ביותר שיש לכם, והחומר כבר כתוב בפרק הרכוש.
כרגע הוא סעיף בתוך עמוד. הוא צריך להיות הכותרת.
