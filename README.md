# התנ״ך הוויזואלי

Static GitHub Pages preview. Serve `site/` with any static HTTP server. No build or package installation is needed. Hebrew homepage and English collection introduction; chapter content remains Hebrew.

The three original interactive chapters are preserved with shared navigation and explicit draft status. The return chapter's unsupported count of differing rows was removed; row alignment and full factual review are still outstanding. Noindex is intentional until editorial review is complete.

Deployment: create a GitHub repository with this directory as its root, enable Pages using GitHub Actions, then manually run Deploy Visual Tanakh. Only `site/` is uploaded. The manual trigger keeps commits from automatically publishing unfinished editorial changes. Add the custom domain after selecting and registering it.

Before public launch: complete claim/quotation review, align grouped census rows, confirm map/data asset attribution and reuse, test English translations as they are added, and replace the working title. Remove noindex after the editorial launch check. Video and social exports have not been produced.
